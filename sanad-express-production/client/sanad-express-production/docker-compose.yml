version: '3.8'

services:
  sanad-express:
    build: .
    container_name: sanad-express-app
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
